export type Men = {
  id: string;
  title: string;
  price: string;
  image: string;
};

export type Women = {
  id: string;
  title: string;
  price: string;
  image: string;
};

export type Kids = {
  id: string;
  title: string;
  price: string;
  image: string;
};
